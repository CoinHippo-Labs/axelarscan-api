const saveBlock = require('./saveBlock');
const saveUptime = require('./saveUptime');
const saveBlockEvents = require('./saveBlockEvents');
const addBlockEvents = require('./addBlockEvents');
const normalizeResult = require('./normalizeResult');

module.exports = {
  saveBlock,
  saveUptime,
  saveBlockEvents,
  addBlockEvents,
  normalizeResult,
};